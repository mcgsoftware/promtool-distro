object-id: service.controller.new-test-method
kind: prompt
label: Add an endpoint to controller
summary: This is a prompt for adding a new endpoint to an existing controller.
published: true
includes:
    - service.techstack
    - service.controller
    - dto.lombok
